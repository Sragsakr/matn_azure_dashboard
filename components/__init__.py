"""Reusable Streamlit UI components (Phase 3 component library).

Every module in this package is safe to import from both app.py and any
page under pages/ — none of them call Streamlit widget functions at import
time (see the Phase 2 lesson documented in core/ui_helpers.py), they only
define functions/constants that render when explicitly called.
"""
